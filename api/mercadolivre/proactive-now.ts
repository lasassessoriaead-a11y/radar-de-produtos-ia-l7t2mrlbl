import { getMercadoLivreSession, requireSupabaseUser } from '../../server/mercadolivre.js'

function clamp(n:number,min:number,max:number){return Math.max(min,Math.min(max,n))}
function scoreCandidate(trendPosition:number,highlightPosition:number,sold:number,price:number,freeShipping:boolean){let score=62;score+=Math.max(2,12-trendPosition);score+=Math.max(2,12-Math.floor(highlightPosition/2));if(sold>=1000)score+=8;else if(sold>=500)score+=6;else if(sold>=100)score+=4;else if(sold>=20)score+=2;if(price>=20&&price<=800)score+=3;if(freeShipping)score+=2;return clamp(Math.round(score),0,96)}
async function mlJson(url:string,token:string){const r=await fetch(url,{headers:{Authorization:`Bearer ${token}`,Accept:'application/json','User-Agent':'RadarIA/1.0'}});const d=await r.json().catch(()=>({}));if(!r.ok){const err:any=new Error(d?.message||d?.error||`Mercado Livre HTTP ${r.status}`);err.status=r.status;err.payload=d;throw err}return d}
function validResolved(x:any){return Boolean(x?.external_id&&x?.title&&x?.product_url&&Number(x?.price||0)>0)}
function productResolved(product:any){const winner=product?.buy_box_winner||{};const price=Number(winner?.price||product?.buy_box_winner_price_range?.min?.price||product?.buy_box_winner_price_range?.min||0);const productUrl=String(product?.permalink||product?.url||product?.short_url||'');const title=String(product?.name||product?.title||'');if(!product?.id||!productUrl||!title||price<=0)return null;return{external_id:String(winner?.item_id||product.id),catalog_product_id:String(product.id),title,product_url:productUrl,image_url:String(product?.pictures?.[0]?.secure_url||product?.pictures?.[0]?.url||product?.thumbnail||'').replace(/^http:/,'https:'),price,sold_quantity:Number(winner?.sold_quantity||product?.sold_quantity||0),category_id:String(winner?.category_id||product?.category_id||''),domain_id:String(product?.domain_id||''),free_shipping:Boolean(winner?.shipping?.free_shipping),resolution:'catalog_product'} }
async function resolveHighlight(entry:any,token:string){const id=String(entry?.id||''),type=String(entry?.type||'').toUpperCase();if(!id)return null;if(type==='PRODUCT'){const product=await mlJson(`https://api.mercadolibre.com/products/${encodeURIComponent(id)}`,token);return productResolved(product)}if(type==='ITEM'&&!id.startsWith('MLBU')){try{const item=await mlJson(`https://api.mercadolibre.com/items/${encodeURIComponent(id)}`,token);if(item?.id&&item?.permalink&&Number(item?.price||0)>0)return{external_id:String(item.id),catalog_product_id:String(item?.catalog_product_id||''),title:String(item.title||''),product_url:String(item.permalink),image_url:String(item?.pictures?.[0]?.secure_url||item?.thumbnail||'').replace(/^http:/,'https:'),price:Number(item.price||0),sold_quantity:Number(item?.sold_quantity||0),category_id:String(item?.category_id||''),domain_id:String(item?.domain_id||''),free_shipping:Boolean(item?.shipping?.free_shipping),resolution:'item'}}catch{}return null}if(type==='USER_PRODUCT'||id.startsWith('MLBU')){try{const up=await mlJson(`https://api.mercadolibre.com/user-products/${encodeURIComponent(id)}`,token);const catalogId=String(up?.catalog_product_id||up?.product_id||'');if(catalogId){const product=await mlJson(`https://api.mercadolibre.com/products/${encodeURIComponent(catalogId)}`,token);const r=productResolved(product);if(r)return{...r,user_product_id:id,resolution:'user_product_catalog'}}}catch{}return null}return null}
async function probeHighlight(entry:any,token:string){const id=String(entry?.id||''),type=String(entry?.type||'UNKNOWN').toUpperCase();let endpoint='';if(type==='PRODUCT')endpoint=`https://api.mercadolibre.com/products/${encodeURIComponent(id)}`;else if(type==='ITEM')endpoint=`https://api.mercadolibre.com/items/${encodeURIComponent(id)}`;else if(type==='USER_PRODUCT'||id.startsWith('MLBU'))endpoint=`https://api.mercadolibre.com/user-products/${encodeURIComponent(id)}`;else return{type,id,status:null,error:'Tipo sem endpoint de diagnóstico'};try{const r=await fetch(endpoint,{headers:{Authorization:`Bearer ${token}`,Accept:'application/json','User-Agent':'RadarIA/1.0'}});const d:any=await r.json().catch(()=>({}));const keys=Object.keys(d||{}).slice(0,25);return{type,id,status:r.status,ok:r.ok,keys,has_name:Boolean(d?.name),has_title:Boolean(d?.title),has_permalink:Boolean(d?.permalink),has_url:Boolean(d?.url),has_winner:Boolean(d?.buy_box_winner),winner_item_id:String(d?.buy_box_winner?.item_id||''),winner_price:Number(d?.buy_box_winner?.price||0),price_range:d?.buy_box_winner_price_range||null,catalog_product_id:String(d?.catalog_product_id||''),product_id:String(d?.product_id||''),message:String(d?.message||d?.error||'').slice(0,180)}}catch(e:any){return{type,id,status:null,error:String(e?.message||e||'Falha de diagnóstico').slice(0,180)}}}
async function saveHistory(ctx:{user:any;auth:string;url:string;key:string},row:any){const r=await fetch(`${ctx.url}/rest/v1/hunter_scan_history`,{method:'POST',headers:{apikey:ctx.key,Authorization:ctx.auth,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(row)});if(!r.ok){const e=await r.json().catch(()=>({}));throw new Error(e?.message||`Falha ao salvar histórico (${r.status}).`)}}

export default async function handler(req:any,res:any){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const ctx=await requireSupabaseUser(req),{user,auth,url,key}=ctx;const {session,setCookie}=await getMercadoLivreSession(req);if(setCookie)res.setHeader('Set-Cookie',setCookie);const trends=await mlJson('https://api.mercadolibre.com/trends/MLB',session.access_token),keywords=(Array.isArray(trends)?trends:[]).map((x:any)=>String(x?.keyword||'').trim()).filter(Boolean).slice(0,8);const rows:any[]=[],seen=new Set<string>(),diagnostics:any[]=[];const typeCounts:Record<string,number>={},resolvedByType:Record<string,number>={},probeCounts:Record<string,number>={};const resolution_probes:any[]=[];let categoriesPredicted=0,highlightsChecked=0,itemsResolved=0,skippedUpstream=0;
for(let i=0;i<keywords.length;i++){const keyword=keywords[i];try{const p=new URL('https://api.mercadolibre.com/sites/MLB/domain_discovery/search');p.searchParams.set('q',keyword);p.searchParams.set('limit','3');const predicted=await mlJson(p.toString(),session.access_token);const cats=(Array.isArray(predicted)?predicted:[]).map((x:any)=>({id:String(x?.category_id||''),domain:String(x?.domain_id||'')})).filter((x:any)=>x.id);categoriesPredicted+=cats.length;let content:any[]=[],categoryId='',domainId='',lastErr:any=null;for(const cat of cats){try{const h=await mlJson(`https://api.mercadolibre.com/highlights/MLB/category/${encodeURIComponent(cat.id)}`,session.access_token),c=Array.isArray(h?.content)?h.content:[];if(c.length){content=c;categoryId=cat.id;domainId=cat.domain;break}}catch(e:any){lastErr=e}}if(!content.length){skippedUpstream++;diagnostics.push({keyword,status:Number(lastErr?.status||0)||null,error:String(lastErr?.message||'Sem ranking de mais vendidos para as categorias previstas.')});continue}highlightsChecked+=content.length;let resolvedForKeyword=0;for(const entry of content.slice(0,12)){const highlightType=String(entry?.type||'UNKNOWN').toUpperCase();typeCounts[highlightType]=(typeCounts[highlightType]||0)+1;if((probeCounts[highlightType]||0)<2){probeCounts[highlightType]=(probeCounts[highlightType]||0)+1;resolution_probes.push({...await probeHighlight(entry,session.access_token),keyword,category_id:categoryId,position:Number(entry?.position||0)})}try{const item=await resolveHighlight(entry,session.access_token);if(!validResolved(item)||seen.has(String(item.external_id)))continue;seen.add(String(item.external_id));itemsResolved++;resolvedForKeyword++;resolvedByType[highlightType]=(resolvedByType[highlightType]||0)+1;const sold=Number(item.sold_quantity||0),price=Number(item.price||0),freeShipping=Boolean(item.free_shipping),position=Number(entry?.position||20),proactiveScore=scoreCandidate(i+1,position,sold,price,freeShipping);rows.push({user_id:user.id,platform:'Mercado Livre',niche_key:String(item.domain_id||domainId||item.category_id||categoryId||'mercadolivre_highlights').toLowerCase(),search_query:keyword,external_id:String(item.external_id),title:String(item.title||keyword),image_url:String(item.image_url||''),product_url:String(item.product_url),affiliate_url:null,price,commission_rate:0,commission_amount:0,sales_count:sold,rating:0,base_score:proactiveScore,niche_confidence:0,proactive_score:proactiveScore,reason:{source:'mercadolivre_trends_highlights_manual_v4',trend_keyword:keyword,trend_position:i+1,highlight_position:position,highlight_type:highlightType,category_id:categoryId,catalog_product_id:item.catalog_product_id||null,user_product_id:item.user_product_id||null,resolution:item.resolution,free_shipping:freeShipping,conversion_learning_enabled:false,affiliate_link_required_after_selection:true,autonomous_prepare_enabled:false},last_seen_at:new Date().toISOString(),updated_at:new Date().toISOString()})}catch(e:any){skippedUpstream++;if(resolution_probes.length<12)resolution_probes.push({type:highlightType,id:String(entry?.id||''),keyword,category_id:categoryId,position:Number(entry?.position||0),resolve_error:String(e?.message||e||'Falha ao resolver').slice(0,180),resolve_status:Number(e?.status||0)||null})}}diagnostics.push({keyword,status:200,category_id:categoryId,highlights:content.length,resolved:resolvedForKeyword})}catch(e:any){skippedUpstream++;diagnostics.push({keyword,status:Number(e?.status||0)||null,error:String(e?.message||e||'Falha desconhecida')})}}
diagnostics.push({summary:true,type_counts:typeCounts,resolved_by_type:resolvedByType,resolution_probes});rows.sort((a,b)=>Number(b.proactive_score)-Number(a.proactive_score));const selected=rows.slice(0,20);if(selected.length){const wr=await fetch(`${url}/rest/v1/hunter_proactive_opportunities?on_conflict=user_id,platform,external_id`,{method:'POST',headers:{apikey:key,Authorization:auth,'Content-Type':'application/json',Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify(selected)});if(!wr.ok){const e=await wr.json().catch(()=>({}));throw new Error(e?.message||'Não foi possível salvar as oportunidades.')}}await saveHistory(ctx,{user_id:user.id,marketplace:'Mercado Livre',run_type:'manual',status:'success',trend_keywords:keywords.length,categories_predicted:categoriesPredicted,highlights_checked:highlightsChecked,items_resolved:itemsResolved,candidates:selected.length,saved:selected.length,skipped_upstream:skippedUpstream,diagnostics,source:'mercadolivre_trends_highlights_manual_v4'});return res.status(200).json({success:true,trend_keywords:keywords.length,categories_predicted:categoriesPredicted,highlights_checked:highlightsChecked,items_resolved:itemsResolved,candidates:selected.length,saved:selected.length,skipped_upstream:skippedUpstream,type_counts:typeCounts,resolved_by_type:resolvedByType,diagnostics,history_saved:true,source:'mercadolivre_trends_highlights_manual_v4'})}catch(e:any){return res.status(500).json({success:false,error:String(e?.message||e||'Falha na busca manual do Mercado Livre.'),status:Number(e?.status||0)||null})}}
