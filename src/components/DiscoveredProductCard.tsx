import React from 'react'
import {ExternalLink,Sparkles,Bookmark,BookmarkCheck,CheckCircle2,XCircle,AlertCircle,FlaskConical,Info,ShieldCheck,Calculator,Cpu,TrendingUp,Trophy} from 'lucide-react'
import {useNavigate} from 'react-router-dom'
import {ScoreRing} from './ScoreRing'
import {OpportunityBadge} from './OpportunityBadge'
import {Button} from '@/components/ui/button'
import type {DiscoveredProductRecord} from '@/types/product'
import {cn} from '@/lib/utils'
interface DiscoveredProductCardProps{product:DiscoveredProductRecord;onApprove:(product:DiscoveredProductRecord)=>void;onDiscard:(product:DiscoveredProductRecord)=>void;onWhyPicked:(product:DiscoveredProductRecord)=>void;onToggleWatchlist:(product:DiscoveredProductRecord)=>void;isWatchlisted?:boolean;isApproving?:boolean;isDiscarding?:boolean;rank?:number}
export const DiscoveredProductCard:React.FC<DiscoveredProductCardProps>=({product,onApprove,onDiscard,onWhyPicked,onToggleWatchlist,isWatchlisted=false,isApproving=false,isDiscarding=false,rank})=>{
 const navigate=useNavigate(),price=product.promo_price&&product.promo_price>0?product.promo_price:product.price,hasPromo=Boolean(product.promo_price&&product.promo_price<product.price&&product.promo_price>0),isHot=product.opportunity_level==='hot',isGood=product.opportunity_level==='good',raw:any=product.raw_data||{},reason:any=raw.reason||{},offersTotal=Number(raw.offers_total||reason.offers_total||0),hasRealPrice=Number(price)>0,hasRealSales=Number(product.sales_count||0)>0,hasRealRating=Number(product.rating||0)>0,isShopee=product.platform==='Shopee'||product.source==='shopee_affiliate_api',isML=product.platform==='Mercado Livre',sourceShort=isShopee?'Shopee':'ML',marketplaceName=isShopee?'Shopee':'Mercado Livre',marketplaceLinkLabel=isShopee?'Ver na Shopee':'Ver no Mercado Livre',trendLabel=String(reason.trend_class_label||''),trendKeyword=String(reason.trend_keyword||''),trendPeriod=String(reason.trend_period||''),trendPosition=Number(reason.trend_position||0),highlightPosition=Number(reason.highlight_position||0),hasOfficialEvidence=isML&&Boolean(reason.rank_evidence&&trendKeyword),hasCommission=Number(product.commission_rate||0)>0,estimatedEarning=hasRealPrice&&hasCommission?Number(price)*Number(product.commission_rate)/100:0,affiliateEligibility=String(reason.affiliate_eligibility||''),affiliateVerified=['eligible','eligible_by_public_rules'].includes(affiliateEligibility)||Boolean(reason.affiliate_eligible===true),affiliateRejected=['ineligible','ineligible_by_public_rules'].includes(affiliateEligibility)||Boolean(reason.affiliate_eligible===false),affiliateLabel=affiliateEligibility==='eligible_by_public_rules'?'Elegível pelos critérios oficiais':affiliateEligibility==='ineligible_by_public_rules'?'Não atende aos critérios oficiais':affiliateVerified?'Elegível confirmada':affiliateRejected?'Não elegível':'Ainda não verificada',canCommercialAction=hasRealPrice&&Boolean(product.product_url)&&(!isML||affiliateVerified)
 const potentialLabel=product.opportunity_score>=80?'Alto Potencial':product.opportunity_score>=60?'Médio-Alto Potencial':product.opportunity_score>=40?'Potencial de Teste':'Baixo Potencial'
 return <div className={cn('group relative flex flex-col justify-between rounded-2xl bg-[#141622] border transition-all duration-300 hover:-translate-y-1 overflow-hidden',isHot?'border-[#FF3D00]/50 shadow-[0_0_20px_rgba(255,61,0,0.15)] hover:border-[#FF3D00]':isGood?'border-[#00E676]/35 shadow-[0_0_15px_rgba(0,230,118,0.12)] hover:border-[#00E676]':'border-[#232738] hover:border-[#00F2FF]/60')}>
  <div className="relative w-full aspect-[4/3] bg-[#0A0B10] overflow-hidden"><img src={product.image_url||'https://img.usecurling.com/p/400/300?q=product'} alt={product.title} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" loading="lazy"/><div className="absolute inset-0 bg-gradient-to-t from-[#141622] via-transparent to-black/70"/>{rank!==undefined&&<div className="absolute top-3 left-3 px-2.5 py-1 rounded-full bg-gradient-to-r from-[#FF3D00] to-[#FF8500] text-[#0A0B10] font-mono font-black text-xs">#{rank}</div>}<div className={cn('absolute top-3 flex items-center gap-1.5 flex-wrap max-w-[75%]',rank!==undefined?'left-14':'left-3')}><OpportunityBadge level={product.opportunity_level} size="sm"/><span className="text-[10px] font-mono font-semibold px-2 py-0.5 rounded-full bg-black/80 text-[#FFE600] border border-[#FFE600]/30">{product.platform||'Mercado Livre'}</span></div><div className="absolute top-2.5 right-2.5 bg-[#0A0B10]/85 p-1 rounded-full border border-white/10"><ScoreRing score={product.opportunity_score} size="sm"/></div></div>
  <div className="p-4 flex-1 flex flex-col justify-between space-y-3"><div><h3 className="text-sm font-bold text-white leading-snug line-clamp-2 mb-1.5">{product.title}</h3><div className="text-[11px] text-gray-400 flex items-center justify-between font-mono"><span>Fonte: <strong className="text-gray-300">{marketplaceName}</strong></span>{product.external_id&&<span className="text-[10px] text-gray-500">ID: {product.external_id}</span>}</div></div>
  {hasOfficialEvidence&&<div className="p-3 rounded-xl bg-[#10180f] border border-[#00E676]/35 space-y-1.5"><div className="text-[10px] uppercase font-black text-[#00E676] flex items-center gap-1"><ShieldCheck className="w-3 h-3"/>Evidência oficial Mercado Livre</div><div className="text-[11px] text-white flex gap-1.5"><TrendingUp className="w-3.5 h-3.5 text-[#FFE600] shrink-0"/><span><b>{trendKeyword}</b> • {trendLabel||'Tendência oficial'}{trendPosition?` • posição ${trendPosition} no /trends`:''}</span></div><div className="text-[11px] text-gray-300 flex gap-1.5"><Trophy className="w-3.5 h-3.5 text-[#FF9D00] shrink-0"/><span>{highlightPosition?`Produto #${highlightPosition} entre os mais vendidos da categoria`:'Presente no ranking oficial de mais vendidos'}</span></div><div className="text-[10px] text-gray-500">Período: {trendPeriod||'semana atual'} • atualização do /trends: semanal</div></div>}
  {isML&&<div className="p-3 rounded-xl bg-[#11131d] border border-[#FFD600]/25 space-y-1.5 text-[10px] font-mono"><div className="uppercase font-black text-[#FFD600]">Afiliados Mercado Livre</div><div className="flex justify-between gap-2"><span className="text-gray-400">Elegibilidade:</span><span className={affiliateVerified?'text-[#00E676]':affiliateRejected?'text-red-400':'text-amber-300'}>{affiliateLabel}</span></div>{reason.seller_reputation_level&&<div className="flex justify-between gap-2"><span className="text-gray-400">Vendedor:</span><span className="text-gray-300">Reputação {String(reason.seller_reputation_level)}</span></div>}{reason.item_condition&&<div className="flex justify-between gap-2"><span className="text-gray-400">Condição:</span><span className="text-gray-300">{String(reason.item_condition)==='new'?'Novo':String(reason.item_condition)}</span></div>}<div className="flex justify-between gap-2"><span className="text-gray-400">Comissão:</span><span className={hasCommission?'text-[#FFD600]':'text-amber-300'}>{hasCommission?`${Number(product.commission_rate).toFixed(2)}%`:'Consultar no Portal do Afiliado'}</span></div><div className="flex justify-between gap-2"><span className="text-gray-400">Você ganha:</span><span className={estimatedEarning>0?'text-[#00E676] font-bold':'text-gray-400'}>{estimatedEarning>0?`R$ ${estimatedEarning.toFixed(2)} por venda`:'Aguardando comissão confirmada'}</span></div></div>}
  <div className="space-y-1.5 py-2 px-2.5 rounded-xl bg-[#0D0F18] border border-[#202538] text-[10px] font-mono"><div className="text-[9px] uppercase tracking-wider text-gray-500 font-bold mb-1 flex items-center gap-1"><Info className="w-3 h-3 text-[#00F2FF]"/>Transparência das Métricas</div><div className="flex items-center justify-between"><span className="text-gray-400">Preço:</span><span className={cn('inline-flex items-center gap-1 px-1.5 py-0.5 rounded border',hasRealPrice?'text-[#00E676] bg-[#00E676]/10 border-[#00E676]/30':'text-gray-400 bg-gray-800/50 border-gray-700')}><ShieldCheck className="w-2.5 h-2.5"/>{hasRealPrice?`Dado Real (${sourceShort})`:'Não fornecido neste endpoint'}</span></div><div className="flex items-center justify-between"><span className="text-gray-400">Vendas:</span><span className="text-gray-300">{hasRealSales?`${product.sales_count} informadas pela API`:hasOfficialEvidence?'Ranking confirma mais vendido; quantidade não informada':'Não informado neste endpoint'}</span></div><div className="flex items-center justify-between"><span className="text-gray-400">Avaliação:</span><span className="text-gray-300">{hasRealRating?product.rating.toFixed(1):'Não informada neste endpoint'}</span></div><div className="flex items-center justify-between"><span className="text-gray-400">Score:</span><span className="inline-flex items-center gap-1 text-[#00F2FF]"><Calculator className="w-2.5 h-2.5"/>Calculado ({product.opportunity_score}/100)</span></div><div className="flex items-center justify-between"><span className="text-gray-400">Potencial:</span><span className="inline-flex items-center gap-1 text-[#C084FC]"><Cpu className="w-2.5 h-2.5"/>{potentialLabel}</span></div>{!isML&&<div className="flex items-center justify-between"><span className="text-gray-400">Comissão:</span>{hasCommission?<span className="text-[#FFD600]">{product.commission_rate.toFixed(2)}%</span>:<span className="text-gray-400 inline-flex gap-1"><AlertCircle className="w-2.5 h-2.5"/>Não fornecida</span>}</div>}</div>
  <div className="space-y-2 pt-1 border-t border-[#232738]"><div className="grid grid-cols-2 gap-2 bg-[#10121A] p-2.5 rounded-xl border border-[#1E2130]"><div><span className="text-[10px] text-gray-400 block">Preço</span><div className="font-mono font-bold text-white text-sm">{hasRealPrice?`R$ ${Number(price).toFixed(2)}`:'Não informado'}</div>{hasPromo&&<span className="text-[10px] text-gray-500 line-through">R$ {product.price.toFixed(2)}</span>}</div><div className="border-l border-[#1E2130] pl-2.5"><span className="text-[10px] text-gray-400 block">Mercado</span><div className="text-[11px] font-mono mt-0.5">{hasOfficialEvidence?<span className="text-[#00E676]">Ranking oficial confirmado</span>:!isShopee&&offersTotal>0?<span className="text-[#FFD600]">{offersTotal} ofertas</span>:<span className="text-gray-400">Dados parciais</span>}</div></div></div>{product.product_url&&<a href={product.product_url} target="_blank" rel="noreferrer" className="w-full h-8 inline-flex items-center justify-center rounded-md border border-[#00F2FF]/35 bg-[#00F2FF]/5 text-[#00F2FF] text-xs font-semibold gap-1.5"><ExternalLink className="w-3.5 h-3.5"/>{marketplaceLinkLabel}</a>}<Button type="button" variant="outline" size="sm" onClick={()=>onWhyPicked(product)} className="w-full h-8 text-xs bg-[#121422] border-[#7000FF]/40 text-[#C084FC] gap-1.5"><Sparkles className="w-3.5 h-3.5"/>Por que a IA escolheu este produto?</Button><div className="grid grid-cols-2 gap-1.5"><Button variant="outline" size="sm" disabled={isDiscarding||isApproving} onClick={()=>onDiscard(product)}><XCircle className="w-3.5 h-3.5 mr-1"/>Descartar</Button><Button variant="outline" size="sm" onClick={()=>onToggleWatchlist(product)}>{isWatchlisted?<BookmarkCheck className="w-3.5 h-3.5 mr-1"/>:<Bookmark className="w-3.5 h-3.5 mr-1"/>}{isWatchlisted?'Salvo':'Monitorar'}</Button>{canCommercialAction&&<><Button size="sm" onClick={()=>navigate(`/laboratorio?discoveredId=${product.id}`)}><FlaskConical className="w-3.5 h-3.5 mr-1"/>Campanha</Button><Button size="sm" disabled={isApproving||isDiscarding} onClick={()=>onApprove(product)}><CheckCircle2 className="w-3.5 h-3.5 mr-1"/>Aprovar</Button></>}</div>{isML&&!affiliateVerified&&<div className="text-[10px] text-amber-300 text-center">Produto real encontrado. A elegibilidade só é marcada quando os critérios públicos do programa são verificados.</div>}{isML&&affiliateVerified&&!hasCommission&&<div className="text-[10px] text-amber-300 text-center">Elegibilidade verificada pelos critérios públicos. A porcentagem exata de comissão continua dependendo do Portal do Afiliado.</div>}</div></div>
 </div>
}
